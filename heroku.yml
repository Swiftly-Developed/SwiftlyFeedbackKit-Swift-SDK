build:
  docker:
    web:
      dockerfile: SwiftlyFeedbackServer/Dockerfile
      context: SwiftlyFeedbackServer
run:
  web: ./SwiftlyFeedbackServer serve --env production --hostname 0.0.0.0 --port $PORT
